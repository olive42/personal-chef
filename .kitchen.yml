---
driver:
  name: vagrant

provisioner:
  name: policyfile_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-16.10

suites:
  - name: default
    verifier:
      inspec_tests:
        - test/smoke/default
    attributes:
      chef_vault:
        databag_fallback: true
